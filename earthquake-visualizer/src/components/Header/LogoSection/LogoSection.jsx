import './LogoSection.css';

const LogoSection = () => {
    return (
        <div className="logo-section">
            <span className="logo-icon">🌍</span>
            <h1 className="app-title">Earthquake Visualizer</h1>
        </div>
    );
};

export default LogoSection;